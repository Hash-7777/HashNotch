name: Build and check

# Runs the same two commands the rules require before a push, on a machine that
# is not the developer's. That is the whole point: everything here has always
# been runnable locally, and something that only ever runs locally is a habit
# rather than a gate.
# On every push to main as well as every pull request. This repository is
# public, where GitHub does not bill for its hosted runners, so the second
# machine costs nothing and anybody reading the repository can see for
# themselves whether what is on main builds and passes.
on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    # Apple Silicon, which is what the app is built for. The app reads
    # Apple Silicon sensors and macOS media interfaces, so an Intel runner
    # would be testing a machine nobody ships to.
    # The image also has to carry Swift 6 or newer, because the package
    # declares tools version 6.0 — an older image cannot read Package.swift
    # at all, let alone build it.
    runs-on: macos-15

    steps:
      - uses: actions/checkout@v5

      - name: Show the toolchain
        run: swift --version

      # Debug and release are built separately on purpose: release turns on
      # optimisation and whole-module builds, and has caught warnings that a
      # debug build does not surface.
      #
      # `set -o pipefail` is the whole gate. Without it the shell reports the
      # exit status of `tee`, which always succeeds, so a build that failed
      # outright passed this step in silence — and the warning grep below did
      # not catch it either, because a toolchain refusing to read the package
      # says `error:` at the start of the line rather than mid-sentence.
      - name: Build (debug)
        run: |
          set -o pipefail
          swift build 2>&1 | tee build-debug.log

      - name: Build (release)
        run: |
          set -o pipefail
          swift build -c release 2>&1 | tee build-release.log

      # The project ships zero warnings and says so. A warning that lands
      # quietly is one nobody reads afterwards.
      - name: Refuse any warning
        run: |
          if grep -E '(^|: )(warning|error): ' build-debug.log build-release.log; then
            echo "::error::The build produced warnings or errors (see above)."
            exit 1
          fi
          echo "No warnings in either configuration."

      # Run once, keep the output, and count from what was kept. Running the
      # checks a second time to count them would let the two disagree, and the
      # count is the thing the badge is judged against.
      - name: Run the checks
        run: |
          set -o pipefail
          swift run HashNotchChecks 2>&1 | tee checks.log

      # The README carries a "checks passing" badge. A badge is a claim, so
      # it has to be true — and it is the kind of claim that rots silently,
      # because adding a check is exactly when nobody thinks about the README.
      - name: The badge must match the checks
        run: |
          counted=$(grep -c '^  ok' checks.log || true)
          claimed=$(grep -oE 'checks-[0-9]+%20passing' README.md | grep -oE '[0-9]+' | head -1)
          echo "checks that ran: $counted"
          echo "README badge:    ${claimed:-<none found>}"
          if [ -z "$claimed" ]; then
            echo "::error::Could not find the checks badge in README.md."
            exit 1
          fi
          if [ "$counted" != "$claimed" ]; then
            echo "::error::The README badge claims $claimed checks but $counted ran. Update the badge and the \"Verified, not asserted\" line."
            exit 1
          fi
          echo "The badge is telling the truth."

      # The hook is copied into people's home folders, so it does not follow an
      # app update. The app now tells them when the copy they have is older
      # than the one it ships — and that notice compares VERSION NUMBERS, on
      # purpose, so that anybody who edited their own copy is never nagged. The
      # cost of that choice is this step: a stamp nobody bumped means a change
      # that ships and never arrives, exactly as the tick-for-stars change did.
      # This is where that mistake gets caught, rather than by pestering the
      # person who did nothing wrong.
      - name: A changed hook must change its version
        run: |
          before=$(git rev-parse "${{ github.event.before }}" 2>/dev/null || echo "")
          if [ -z "$before" ] || ! git cat-file -e "$before^{commit}" 2>/dev/null; then
            echo "No previous commit to compare against; nothing to check."
            exit 0
          fi
          hook=scripts/claude-code-hook.sh
          if git diff --quiet "$before" HEAD -- "$hook"; then
            echo "The hook did not change."
            exit 0
          fi
          old_version=$(git show "$before:$hook" 2>/dev/null | sed -n 's/^HOOK_VERSION=\([0-9][0-9]*\).*/\1/p' | head -1)
          new_version=$(sed -n 's/^HOOK_VERSION=\([0-9][0-9]*\).*/\1/p' "$hook" | head -1)
          echo "hook version: ${old_version:-<none>} -> ${new_version:-<none>}"
          if [ -z "$new_version" ]; then
            echo "::error::$hook has no HOOK_VERSION= stamp. The app cannot tell people their copy is stale without one."
            exit 1
          fi
          if [ "$old_version" = "$new_version" ]; then
            echo "::error::$hook changed but HOOK_VERSION is still $new_version. Bump it, or everybody who already installed the hook keeps the old behaviour with nothing to tell them."
            exit 1
          fi
          echo "The stamp moved with the hook."

      # Proves the thing that actually gets shipped assembles and that its
      # signature verifies — the failure that would otherwise reach every
      # person who downloads it. Ad-hoc signed here, as on the developer's Mac;
      # no certificate or secret is needed, and none is available to this job.
      - name: Assemble the app bundle
        run: ./scripts/build_app.sh

      - name: Confirm the bundle is what it claims
        run: |
          app="build/HashNotch.app"
          codesign --verify --strict --verbose=2 "$app"
          version=$(/usr/libexec/PlistBuddy -c 'Print :CFBundleShortVersionString' "$app/Contents/Info.plist")
          echo "Built and verified HashNotch $version"
